version: '3.8'

services:
  rotula4:
    image: ghcr.io/pablosud52/rotula4:latest
    build: .
    container_name: rotula4
    restart: unless-stopped
    ports:
      - "1010:8080"
    environment:
      - TZ=America/Montevideo
      - PORT=8080
      - DB_PATH=/app/data/etiquetas.db
      - UPLOADS_DIR=/app/uploads
    volumes:
      - ./data:/app/data
      - ./uploads:/app/uploads
